namespace FaceRecognizer.Common.Enums
{
    public enum ErrorHttpStatus
    {
        WARNING = 200,
        VALIDATION = 400,
        UNAUTHORIZED = 401,
        FORBIDDEN = 403,
        NOT_FOUND = 404,
        FAILED_LOGOUT = 422,
        INTERNAL = 500
    }
}
